#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to multiply with first number
 * Return: the result of multiplication a parameter and b parameter
 */
int mul(int a, int b)
{
	return (a * b);
}
